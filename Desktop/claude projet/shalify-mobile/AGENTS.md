# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v52.0.0/ before writing any code.

> Note : l'app utilise Expo SDK 52 (voir `app.json` : `"sdkVersion": "52.0.0"`). Ne pas suivre des docs SDK 53+ avant migration explicite.
